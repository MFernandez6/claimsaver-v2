"use client";

// This component is now disabled - session management is handled directly in dashboard
export default function SessionManager() {
  // This component doesn't render anything
  return null;
}
